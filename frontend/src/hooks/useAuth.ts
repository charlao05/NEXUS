/**
 * useAuth — Hook standalone (LEGACY)
 * ====================================
 * Re-exporta o useAuth do AuthContext para manter compatibilidade.
 * Novos componentes devem importar direto de '../contexts/AuthContext'.
 */

export { useAuth } from '../contexts/AuthContext'
