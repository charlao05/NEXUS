# Dependências de TESTE — declaradas, não assumidas.
#
# POR QUE ESTE ARQUIVO EXISTE (26/07/2026):
# O NEXUS CI falhou 134 vezes seguidas desde 02/03/2026. Parte da causa raiz:
# o workflow instalava apenas `pytest httpx`, enquanto a suíte depende também
# de pytest-asyncio — que existia nas máquinas de desenvolvimento e em nenhum
# outro lugar. Resultado: 4 testes de tests/test_gov_integrations.py falhavam
# com "async def functions are not natively supported" SÓ no CI, e a suíte
# passava localmente.
#
# É o mesmo padrão das variáveis de ambiente ausentes do render.yaml:
# dependência que existe na prática mas não está declarada em lugar nenhum.
#
# Instalar com:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27

# Automação de browser (tests/test_browser_reliability.py e o subsistema
# backend/browser/). O runner do CI não instala os binários do Chromium —
# `playwright install chromium` é passo separado do build.
pytest-playwright>=0.5
